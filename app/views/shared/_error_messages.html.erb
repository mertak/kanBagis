<% if @donor.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      Bu formda <%= p (@donor.errors.count)%> hata var.
    </div>
    <ul>
    <% @donor.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
